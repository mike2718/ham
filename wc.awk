# 用于统计文本文件中的：
#   - 行数 (l)
#   - 单词数 (w，以空格分隔)
#   - 字符数 (c，包括空格)

{
    # 主处理块
    l = 1       # 启用行数统计
    w = 1       # 启用单词数统计
    c = 1       # 启用字符数统计
    
    wc += NF    # 累计单词数 (NF = 当前行的字段数)
    cc += length($0)  # 累计字符数 (包括空格)
}

END {
    # 结束处理块
    printf "| %s | %s | %s |\n", 
        l ? NR : "",   # 行数 (如果l为真则输出NR)
        w ? wc : "",   # 单词数 (如果w为真)
        c ? cc : ""    # 字符数 (如果c为真)
}